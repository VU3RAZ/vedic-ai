# Complete install — equivalent to: pip install -e ".[dev,engine,retrieval,llm,api]"
# Alternatively use:  pip install -e ".[dev,engine,retrieval,llm,api]"

# Core
pydantic>=2.0
pydantic-settings>=2.0
PyYAML>=6.0
typer>=0.12
structlog>=24.0
rich>=13.0

# Astrology engine
flatlib
pyswisseph

# Retrieval (RAG)
sentence-transformers
faiss-cpu

# LLM layer
httpx>=0.27
json5

# API / web server
fastapi>=0.110
uvicorn[standard]>=0.29

# Dev / test
pytest>=8.0
pytest-snapshot
